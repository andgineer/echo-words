[pytest]
addopts = --doctest-modules
# experiments/ is the sanctioned non-CI harness: it imports anki and llmbroker,
# which are not project dependencies, so a bare `pytest` must not collect it.
testpaths = tests
